/* global glue  describe it before after expect */

// TODO: test cases:
// 1. obj types filter: ["a", "b"] in ["a", "b", "c"] isMatch and ["a"] is not match
// 2. full method definition filter
// 3. methods()[smth] applied to a filter

// NEEDS TWO APPLICATIONS
describe('methods()', function () {

    describe('AGM discovery: ', function () {

    });
});